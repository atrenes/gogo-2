server:
  port: 8080
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        #TODO
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/